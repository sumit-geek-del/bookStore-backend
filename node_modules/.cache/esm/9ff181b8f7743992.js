let Sequelize,DataTypes;_f77‍.x([["dbConnection",()=>db]]);_f77‍.w("sequelize",[["Sequelize",["Sequelize"],function(v){Sequelize=v}],["DataTypes",["DataTypes"],function(v){DataTypes=v}]]);

const sequelize = new Sequelize('bookstore', 'root', '', {
    host: 'localhost',
    dialect: 'mysql',
    logging:false
  });

const db = {};
db.Sequelize = Sequelize;
db.sequelize = sequelize;


  try {
    sequelize.authenticate();
    console.log('Connection has been established successfully.');
  } catch (error) {
    _f77‍.g.console.error('Unable to connect to the database:', error);
  }

db.users = require('../models/Users/usersModel')(sequelize, DataTypes);
db.books = require("../models/books/booksModel")(sequelize, DataTypes);
  
db.sequelize.sync();
console.log("All models were synchronized successfully.");


